import 'package:flutter/material.dart';

class TopInstrucors extends StatelessWidget {
  const TopInstrucors({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
